export enum STATUS_ROOM {
  WAITING = "waiting",
  STARTED = "started",
  PLAYING = "playing",
  PAUSED = "paused",
  ENDED = "ended",
}

export enum STATUS_HOST {
  KEEP = "keep",
  LEAVE = "leave",
}
